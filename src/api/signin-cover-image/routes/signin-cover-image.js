'use strict';

/**
 * signin-cover-image router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::signin-cover-image.signin-cover-image');
